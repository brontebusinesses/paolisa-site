---
/**
 * <Wordmark /> — Marque Paolisa avec point ●.
 * Raleway + point noir (currentColor), rapetissé et resserré via
 * .wordmark-dot dans global.css pour éviter l'effet "gros pavé" du
 * glyphe ● à taille pleine (visible à l'export et à l'impression).
 */
type Props = {
  size?: 'sm' | 'md' | 'lg';
  inverse?: boolean;
  class?: string;
  as?: 'span' | 'h1' | 'div';
};

const {
  size = 'md',
  inverse = false,
  class: className = '',
  as = 'span',
} = Astro.props as Props;

// sm = product cards, mentions secondaires.
// md = header, footer, menu overlay, présence marque.
// lg = grandes occurrences éditoriales.
const sizeMap: Record<string, string> = {
  sm: 'text-[15px] md:text-[17px]',
  md: 'text-[19px] md:text-[22px]',
  lg: 'text-[28px] md:text-[36px]',
};

const colorClass = inverse ? 'text-ink-inverse' : 'text-ink-primary';
const Tag = as;
---

<Tag
  class:list={[
    'wordmark inline-flex items-baseline leading-none',
    sizeMap[size],
    colorClass,
    className,
  ]}
>
  <span>PAOLISA</span><span class="wordmark-dot" aria-hidden="true">●</span>
</Tag>
